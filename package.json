{
  "name": "ytplus-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "echo yt.plus > ./dist/CNAME && gh-pages -d dist",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^4.4.5"
  },
  "dependencies": {
    "gh-pages": "^6.0.0",
    "node": "^20.5.0"
  }
}
